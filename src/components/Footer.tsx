import React from 'react'
  import * as Styles from './Footer.styles'

  type PropsType = {}
  const Footer = ({}:PropsType) => {
  return <Styles.FooterWrapper></Styles.FooterWrapper>
}

export default Footer
