import React from 'react'
  import * as Styles from './Home.styles'

  type PropsType = {}
  const Home = ({}:PropsType) => {
  return <Styles.HomeWrapper></Styles.HomeWrapper>
}

export default Home
